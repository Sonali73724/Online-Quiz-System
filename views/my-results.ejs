<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user_name %>'s Quiz Results - Quizlina</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .results-container {
            max-width: 800px;
            margin: 2rem auto;
            padding: 2rem;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .results-header {
            text-align: center;
            margin-bottom: 2rem;
        }
        .results-header h1 {
            color: #1a365d;
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
        }
        .results-header p {
            color: #4a5568;
            font-size: 1.2rem;
        }
        .results-table {
            width: 100%;
            border-collapse: collapse;
        }
        .results-table th, .results-table td {
            padding: 1rem;
            text-align: left;
            border-bottom: 1px solid #e2e8f0;
        }
        .results-table th {
            background-color: #f8f9ff;
            font-weight: bold;
            color: #1a365d;
        }
        .results-table tr:hover {
            background-color: #f8f9ff;
        }
        .score {
            font-weight: bold;
            color: #2c5282;
        }
        .no-results {
            text-align: center;
            color: #4a5568;
            font-style: italic;
        }
       .menu {
            background-color: black;
            padding:20px;
        }
    
    </style>
</head>
<body>
        <div class="menu">
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/quize">Quizes</a></li>
                <li><a href="/logout">Logout</a></li>
            </ul>
        </div>

    <div class="results-container">
        <div class="results-header">
            <h1>Welcome, <%= user_name %>!</h1>
            <p>Here are your quiz results:</p>
        </div>

        <% if (results.length > 0) { %>
            <table class="results-table">
                <thead>
                    <tr>
                        <th>Score</th>
                        <th>Total Questions</th>
                        <th>Date</th>
                    </tr>
                </thead>
                <tbody>
                    <% results.forEach(result => { %>
                        <tr>
                            <td class="score"><%= result.score %></td>
                            <td><%= result.total_questions %></td>
                            <td><%= new Date(result.created_at).toLocaleString() %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } else { %>
            <p class="no-results">You haven't taken any quizzes yet. Start now!</p>
        <% } %>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        // Add any client-side JavaScript here if needed
    </script>
</body>
</html>